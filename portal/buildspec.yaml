version: 0.2

phases:

  install:
    commands:
      - echo Installing dependencies

  build:
    commands:
      - echo Deploying portal
      - aws s3 sync developer-portal/ s3://platform-developer-portal

  post_build:
    commands:
      - echo Invalidating CloudFront cache
      - aws cloudfront create-invalidation --distribution-id $CF_DIST_ID --paths "/*"


#       Key step:

# aws s3 sync developer-portal/ s3://bucket

# Uploads updated portal files.
